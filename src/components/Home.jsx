import { useNavigate } from "react-router";

const Home = () => {
    const navigate = useNavigate()
    navigate('/login')
    return (
        <div>
          
        </div>
    );
};

export default Home;